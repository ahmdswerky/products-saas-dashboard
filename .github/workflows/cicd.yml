name: Deploy to Server

on: 
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@master
      - uses: yarn install
      - uses: yarn test:unit
      - uses: yarn build